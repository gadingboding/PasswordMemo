支持数据导入功能，目前以及支持数据导出功能，用户可以通过导入功能将之前导出的数据恢复到当前浏览器中。
导入和到处功能的按钮应该放在一起，水平对齐，并且图标风格相似。
用户操作流程：
1. 点击导入按钮
2. 弹出文件选择器
3. 确认是否导入，导入会清除目前的全部数据，包括清除webdav相关配置
4. 覆盖当前的vault-data数据，并清空user-profile数据
5. 返回入口页面，用户需要重新登录

